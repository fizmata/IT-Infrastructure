---
  - name: install docker
    apt:
      name:
        - docker.io
        - python3-docker
      state: present
      

  - name: Enabled
    service:
      name: docker
      state: started
      enabled: yes
