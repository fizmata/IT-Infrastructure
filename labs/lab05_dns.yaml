---
  - name: Collect info about all VMs
    hosts: all
    tasks:
      - setup:

  - name: DNS server
    hosts: dns_servers
    become: yes
    roles:
      - bind

  - name: set DNS
    hosts: all
    become: yes
    roles:
      - configure_resolved
