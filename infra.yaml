---
  - name: Collect info, update apt cache
    hosts: all
    become: yes
    tasks:
      - setup:
    roles:
      - init
 
  - name: dns servers
    hosts: dns_servers
    become: yes
    gather_facts: no
    roles:
      - bind
      - bind_exporter
    tags:
      - bind
      - dns
